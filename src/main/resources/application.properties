# -----------------------------
# Spring Boot Application Name
# -----------------------------
spring.application.name=cartmodule

# -----------------------------
# MySQL Database Configuration
# -----------------------------
# Replace 'cartdb', 'root', and 'your_mysql_password' with your database name, MySQL username, and password
spring.datasource.url=jdbc:mysql://localhost:3306/cartdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Dspsn@20041202
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -----------------------------
# JPA / Hibernate Configuration
# -----------------------------
# Automatically create/update tables based on entities
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in console (optional but useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate Dialect for MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# -----------------------------
# Server Port (optional)
# -----------------------------
server.port=8085
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.max-age=3600
logging.level.org.springframework.web=DEBUG
spring.security.csrf.enabled=false
spring.thymeleaf.check-template-location=false

spring.security.user.name=admin
spring.security.user.password=Dspsn20041202